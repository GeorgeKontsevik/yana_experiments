# yana_experiments

Repository in the dissertation project workspace.

## Setup

Use the dependency manifest in this repository:

- `pyproject.toml` for uv/PEP 621 projects
- `requirements.txt` for requirements-based projects
- `uv.lock` when present

## Agent Instructions

`AGENTS.md` is installed from the shared `agent-config` repository.
